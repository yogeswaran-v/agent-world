# Disable npm progress bars and audit for faster installs
progress=false
audit=false
fund=false

# Use faster registry mirror (optional)
# registry=https://registry.npmmirror.com/

# Skip optional dependencies that might cause issues
optional=false

# Prefer offline packages when available
prefer-offline=true